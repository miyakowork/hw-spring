<mapper namespace="com.lagou.dao.IUserDao">


    <!--sql的唯一标识：namespace.id来组成 ： statementId-->
    <select id="findAll" resultType="com.lagou.pojo.User">
        select *
        from user
    </select>


    <!--
        User user = new User()
        user.setId(1);
        user.setUsername("zhangsan")
    -->
    <!--老师之前拼错了parameter，我修改了下-->
    <select id="findByCondition" resultType="com.lagou.pojo.User" parameterType="com.lagou.pojo.User">
        select *
        from user
        where id = #{id}
        and username = #{username}
    </select>


    <select id="findById" resultType="com.lagou.pojo.User" parameterType="com.lagou.pojo.User">
        select *
        from user
        where id = #{id}
    </select>


    <update id="updateNameById" parameterType="com.lagou.pojo.User">
        update user
        set username = #{username}
        where id = #{id}
    </update>

    <insert id="insertUser" parameterType="com.lagou.pojo.User" useGeneratedKeys="true">
        insert into user (username, `password`, birthday)
        values (#{username}, #{password}, #{birthday});
    </insert>

    <delete id="deleteById" parameterType="com.lagou.pojo.User">
        delete
        from user
        where id = #{id}
    </delete>
</mapper>